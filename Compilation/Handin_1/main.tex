%%%%%     PACKS     %%%%%
\documentclass[12pt]{article}
\usepackage[margin=1in,headsep=.60in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[table, dvipsnames]{xcolor}
\usepackage{array}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{mdframed} %For box around text
\usepackage{hyperref} %For hyperlinks in the PDF
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
}
\urlstyle{same}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{siunitx}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{pgfplots}
\graphicspath{{Images/}}
\usepackage[colorinlistoftodos]{todonotes}
\usepackage{cleveref}
\usepackage[labelformat=simple]{subcaption}
\usepackage{grffile}
\usepackage{gensymb}
\usepackage{float}
\usepackage[shortlabels]{enumitem}
\usepackage{enumitem}
\setlistdepth{9}
\usepackage{biblatex} %Imports biblatex package
\addbibresource{references.bib}
\usepackage{outlines}
\usepackage{minted}
\usepackage{pdflscape}
\usepackage{everypage}
\usepackage{multirow}
\usepackage{multicol}
\usepackage{afterpage}
\usepackage[labelfont=bf, font=small]{caption}
\usepackage{upgreek}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{listings}

% style sheet for code
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}

\lstset{style=mystyle}

\usepackage{lastpage}
%%%%%     COMMANDS     %%%%%
%%%% Blue box for subsection text (not figures) %%%%
\newenvironment{bluebox}
  {\begin{mdframed}[backgroundcolor=blue!5,linecolor=blue!40,roundcorner=8pt]}
  {\end{mdframed}}

%%%% Simple neutral box for figures %%%%
\newenvironment{figbox}
  {\begin{mdframed}[roundcorner=8pt,shadow=true,shadowsize=4pt,shadowcolor=black!40]}
  {\end{mdframed}}

\begin{document}

\begin{titlepage}
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}

\center

\textsc{\LARGE Aarhus University}\\[1.5cm]
\textsc{\Large Department Of Computer Science }\\[0.5cm]
\textsc{\large Compilation}\\[0.5cm]
    

\HRule\\[0.4cm]
    \center 
    {\huge\bfseries  Hand-in 1}\\[0.4cm] % Title of your document
\HRule\\[1.5cm]

\begin{minipage}{0.4\textwidth}
        \begin{flushleft}
            \large
            \textit{Author}\\   
            Søren M. \textsc{Damsgaard}\\
                 % Your name
        \end{flushleft}
    \end{minipage}
~
    \begin{minipage}{0.4\textwidth}
        \begin{flushright}
            \large
            \textit{Student number}\\
            \textbf{202309814}\\
                 % Studienummer\
            
        \end{flushright}
    \end{minipage}

\vfill\vfill\vfill % Position the date 3/4 down the remaining page
    
    {\large\today}

\vfill\vfill
    \includegraphics[width=0.2\textwidth]{Aarhus_University_seal.png}\\[1cm] % Include a department/university logo - this will require the graphicx package
    

\vfill
\end{titlepage}
%%%%%      CHAPTERS      %%%%%
\hspace{0.02cm}
\section*{Goofy ah Math}
\subsection*{Evaluator}
\subsubsection*{Question 1}
Does this function need to recursively explore its argument, and why (or why not)?\\\\
The function \textmd{eval} explicetly uses recursion to make it possible for BinOp to chain expressions, allowing for longer expressions with multiple operations.
\subsubsection*{Question 2}
Why does this function have the return type int? What other return types may be
suitable?\\\\
Since the function inout is an 'expr' and its type is defined as reuslting in a $Int$ via either $Int \rightarrow Int$ or its $BinOp$ which eventually leads to an $Int$.\\
Other thibgs that might work, would be types such as $Float$ for more precise $Div$ operations, or it could also return $None$ incase of some error, like division by zero.
\subsubsection*{Question 3}
How does your evaluation handle the case of division by zero? Note that it may be just
fine to not special-treat division by zero, but it is important you understand what actually
happens at runtime.\\\\
No work was put towards treating the division by zero case, since it's compiled by the in-house OCaml compiler
 it will raise an exception right now, but if the compiler wasn't there to handle it, there will be a funreal.

\subsection{Pretty Printer}
\subsubsection*{Question 4}
Are the functions you have defined recursive, and why (or why not)?\\\\
The function is recursive for the same reason as the evaluator function is recursive, it has to traverse down
 the AST to find the lowest $Int$ and make sure to print from the left side of the tree, while it slowly bubbles
  up througout the tree.  
\subsubsection*{Question 5}
Skipped this one. -Aura \#StupidestProgrammerInTheNorth \#ReadyToBeWhippedIntoShape
\subsubsection*{Question 6}
What is the OCaml type of asm\_example?\\\\
The type is $elem_list$, but it can be traced to the type $asm$ in the $x86.ml$ file, where it is defined as lists of various datatypes such as opcodes, operand lists and ascii, so it's appropiate to call it a list of elements.  
\subsubsection*{Question 7}
How would you rewrite this to use the Asm module without the local module open directive?\\\\
The local module open directive can be replaced by dot notation instead, as an example for accessing $gtext$ in dot notation it could be
$X86.Asm.gtext$, this can be used for the whole statement.
\[
(X86.Movq,\; [X86.Asm.(~\$)\; 23; \; X86.Asm(~\%) \; X86.Rax])
\]
This is ugly, so the local module open directive is prefered. Rather it's more readable.
\subsubsection*{Question 8}
Could we have used text instead of gtext? Why?\\\\
The $gtext$ and $text$ are helper functions from $x86$ but they only differ in one aspect,
 they defined the block of data/code as either global or local, hence the $g$ in $gtext$, this essentially 
 defines if the data/code is public or private, if it's available for other modules to call upon or not, so $gtext$ for Social, or $text$ for shy
\subsubsection*{Question 9}
What output do you get from echo \$? when returning the value 2023?\\\\
Changing the assembly literal to 2023 makes function output 231, the reasoning is for this is explained in the assignment itself, POSIX only reserve only one byte for the return value, hence the cut-off  

\subsection{Translattion}

\begin{align}
&20 + 26 * 58 = 1528 \\
&5 * 1 / 10 = 0\\
&31 - 870 = -839\\
&6 / 10 + 49 + 10 * 70 * 77 - 12 / 9 + 5 = 53940\\
&34 / 72 - 17 / 46 = 0
\end{align}


\printbibliography
\end{document}